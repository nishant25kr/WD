<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white" >
            <form action="/create" method="post">
                <input type="text"class="block bg-zinc-800 px-5 py-2 w-full rounded-lg outline-none" placeholder="Title" name="title">
                <textarea class="block px-5 py-2 resize-none bg-zinc-800 mt-2 w-full rounded-lg outline-none" placeholder="Write your task here" id="" name="details"></textarea>
                <input type="submit" value="Create Task" class="mt-2 bg-blue-700 px-5 py-2 rounded-lg">

            </form>

        </div>
        
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if (files.length>0){%>
                <% files.forEach(function(val){%>
                    <div class="task w-72 px-3 py-4 rounded-lg bg-zinc-800  ">
                        <h1 class="text-white text-2xl tracking-tighter"><%=val%></h1>
                        <div class="flex w-full justify-between item-center mt-3">
                            <a href="/file/<%= val%>" class="text-blue-500">Read more.</a>
                            <a href="/edit/<%= val%> " class="text-blue-500">edit</a>
                        </div>


                    </div>
                
                <%})%>

            <%}else{%>
                <h3 class="text-white">No task here</h3>
            <%}%>    
        </div>
    </div>
</body>
</html>
